# 铁矿检测 RAG 项目文档入口

本文是本课题在新会话中的唯一固定入口。先读本页确认当前阶段，再按任务打开一个阶段文档或一条改动详情；不要为了恢复上下文而从头阅读全部文档。

## 当前状态

| 项目 | 当前值 |
| --- | --- |
| 上游基线 | Ragent `1.1.0`，`f64de341452c8998ebf64cd264e60ccad6a31631` |
| 开发分支 | `research/iron-ore-rag` |
| 当前阶段 | 第一版 MVP 工程收尾、页面验收仍待；小型系统评测、XLSX 分块和第一轮检索纯度优化已完成 |
| 基线标签 | `checkpoint/iron-ore-rag-baseline-1.1.0` |
| 最近代码检查点 | `ed2590e`（XLSX 结构感知分块）、`fd538a8`（请求级检索预算与结构化 rerank） |
| 下一动作 | 页面验收作为独立待办继续；若继续评测，先解决子问题去重后的请求额度补位并固定改写输出，再做至少三次重复运行，不再简单增加 TopK |

阶段 0/1 已完成，阶段 2 已实现 XLSX 解析、精确来源、严格检索、候选任务、人工批准、模拟执行和确定性版本差异。阶段 3 又增加了确定性的机器人任务编译器、ROS1 Noetic Action、dry-run 网关、前端派发/反馈/取消与独立审计表。评测后又修复了 XLSX 超长/重复块和多子问题 TopK 膨胀问题。ROS1 成功、取消和拒绝非 dry-run 的运行时路径已验证，本机数据库迁移已应用；还需要重启 IDEA 后端并从登录页面完成最终联调。设备报警资料仍缺失，真实设备控制继续不在当前范围。

## 冻结小型评测进度

- B0 已完成 3 份文档、24 题检索和 24 个完整回答：21 道可回答/近领域题的锚点 Hit@5 为 `95.2%`，解析锚点恢复 `13/15`，检索 P95 为 `17.5 s`。
- B1 的 Top-1 意图准确率为 `94.4%`，但锚点 Hit@5 没有提升，全部锚点 Hit@5、路由纯度和 P95 均变差，因此暂不把意图开启视为收益。
- B2 开启 OCR 后，原生 PDF 的解析锚点由 `3/5` 增至 `4/5`、该族 Hit@5 由 `83.3%` 增至 `100%`；扫描 PDF 维持 `5/5` 和 `100%`。当前版也出现了相同的解析锚点变化，但这些各一次运行的小样本观察仍不外推。
- 当前版 C1 的 Top-1 意图准确率为 `100%`，相对 C0 将锚点 Hit@5 从 `85.7%` 提至 `90.5%`，但路由纯度从 `66.8%` 降至 `65.3%`，未通过固定选型门槛。
- 当前版 C2 将原生 PDF 解析锚点从 `3/5` 提至 `4/5`，扫描 PDF 仍为 `5/5`；因扫描件没有新增锚点，OCR 也未通过固定门槛。选择器最终固定为 `intent=off, ocr=off`。
- C-final 的锚点 Hit@5 为 `95.2%`、Anchor Recall 为 `86.5%`、检索 P95 为 `8.5 s`；24/24 个完整回答和 24/24 条 trace 成功，回答 P95 为 `22.6 s`。
- 隐藏实验臂的 48 条人工严格判定已完成：B0 与 C-final 总体严格通过率均为 `79.2%`，可回答题均为 `76.2%`，3 道不可回答题均为 `100%` 正确拒答；两组逐题标签完全一致，因此结论是维持基线质量，而非回答准确率提升。
- C0 与 C-final 使用同一关闭配置和同一冻结索引，却分别得到 `85.7%` 与 `95.2%` 的锚点 Hit@5，说明在线改写/模型链路存在运行波动；本轮数字必须连同样本量和重复性限制一起表述。
- XLSX 暴露出更明确的分块问题：目标为 1024 字符，但 123 块中 70 块超限、最大 12,489 字符，精确重复块比例约 `13.8%`。
- `ed2590e` 将同一 XLSX 从 123 块重建为 79 块，最大正文由 12,489 降到 1,019，超过 1,024 字符的块由 70 降为 0，多余精确重复块由 17 降为 0，固定解析锚点仍为 5/5。
- `fd538a8` 在不重建 C-final 索引的 D0 对照中，将平均返回块数从 `13.05` 降到 `6.52`、最大值从 19 限制为 10，Context Precision 从 `17.1%` 提到 `29.2%`，文档召回保持 `97.6%`；自动 Anchor Recall 从 `86.5%` 小降到 `84.1%`，其中唯一新增的 Hit@5 失败经人工检查已返回正确标准标题，但未命中冻结字符串锚点。
- D1 干净重建后，XLSX 六题 Hit@5（至少一个）保持 `100%`，Context Precision 为 `39.3%`、Anchor Recall 为 `94.4%`；两份 PDF 的远端解析块数从冻结的 14/4 漂移为 16/6，因此 D1 总体下降不归因于本次代码。
- 基线数据库停留在 B0；当前数据库停留在 D1 并已保存本地快照，两个后端都已停止。冻结结果、D0/D1、盲评表和 dump 只保存在 Git 忽略目录。详情见[小型系统评测工具](changes/2026-08-13-system-evaluation-harness.md)、[XLSX 结构感知分块与去重](changes/2026-08-13-xlsx-structure-aware-chunking.md)和[请求级检索预算与结构化重排](changes/2026-08-13-request-level-retrieval-purity.md)。

## 第一版 MVP 收尾结论

- 工程实现已固定在提交 `a016f01`，本地收尾标签为 `checkpoint/iron-ore-rag-demo-mvp`。
- 已验证 21 个针对性 Java 测试、前端生产构建、ROS1 catkin 构建、任务成功/取消/拒绝非 dry-run，以及完整 PostgreSQL Schema 初始化。
- 尚未完成的是登录页面中的人工点击验收；这不等同于代码缺失，下次只需按阶段 2/3 文档启动并走一遍固定流程。
- 当前成果只能描述为“有文档依据和人工审批的候选任务，确定性编译为白名单 ROS1 dry-run 任务”，不能描述为自主控制真实机器人。
- 报警排查、真实设备、强化学习与通用复杂表格能力均不属于本检查点。

“MVP 工程收尾”表示当前代码与文档已经形成可恢复检查点，不表示真实设备集成或页面验收已经完成。

## 按需阅读

| 目的 | 阅读内容 |
| --- | --- |
| 新会话恢复当前进度 | 本页 → [阶段 3：ROS1 机器人任务 Demo](stages/03-ros1-robot-mission-demo.md) |
| 重跑已完成的本地基线 | [阶段 0：本地基线复现](stages/00-reproduction.md) |
| 了解课题目标、架构与能力边界 | [项目背景与源码上下文](../iron-ore-robot-rag-project-context.md) |
| 在另一台环境重新跑通项目 | [阶段 0：本地基线复现](stages/00-reproduction.md) → [本地中间件说明](../../resources/docker/dev/README.md) |
| 查看已经改过什么 | [改动索引](changes/README.md) |
| 运行 3 文档 / 24 题小型系统评测 | [评测方法与工具](../../eval/iron-ore/README.md) → [固定运行手册](../../eval/iron-ore/RUNBOOK.md) |
| 查看 XLSX 与检索纯度优化证据 | [XLSX 结构感知分块](changes/2026-08-13-xlsx-structure-aware-chunking.md) → [请求级检索纯度](changes/2026-08-13-request-level-retrieval-purity.md) |
| 部署和录制当前 Demo | [阶段 2：工业知识闭环 Demo](stages/02-industrial-knowledge-demo.md) |
| 启动并验收 ROS1 搬运仿真 | [阶段 3：ROS1 机器人任务 Demo](stages/03-ros1-robot-mission-demo.md) |
| 查看刚完成的取消修复 | [取消后 trace run 悬挂修复](changes/2026-08-12-cancel-trace-run-hang.md) |
| 追溯阶段 0 的详细验证证据 | [阶段 0 实施记录（归档）](../iron-ore-rag-implementation-log.md) |
| 查看教程意图节点 SQL | [意图树教程示例](../../resources/database/examples/intent_node_tutorial.sql) |

## 阶段路线

阶段文档只保留目标、前置条件、复现或验收方法、注意事项和停止点。实施细节放进独立改动记录，不在阶段文档中累积流水账。

| 阶段 | 状态 | 目标 | 文档 |
| --- | --- | --- | --- |
| 0. 项目分析与基线复现 | 已完成 | 固定 1.1.0 基线，建立本地中间件，跑通最小 RAG 闭环 | [复现说明](stages/00-reproduction.md) |
| 1. 最小样本与评测基线 | 已完成 | 用一个脱敏浓度检测样本对照验证 XLSX 与 Markdown，固定问题集和停止线 | [评测说明](stages/01-table-sample-evaluation.md) |
| 2. 工业知识闭环 MVP | 工程已收尾，待页面验收 | 版本化入库、精确引用、候选模板、人工审核、模拟执行和版本差异 | [实施与验收](stages/02-industrial-knowledge-demo.md) |
| 3. ROS1 机器人任务 Demo | 工程已收尾，待页面验收 | 将批准任务确定性编译为白名单搬运技能，通过 ROS1 Action dry-run 派发、反馈和取消 | [实施与验收](stages/03-ros1-robot-mission-demo.md) |
| 4. 真实机器人与设备集成 | 条件未具备 | 在接口和安全责任明确后接入感知、控制及实验设备 | 开始阶段时创建 |

产品能力仍以阶段 2/3 的固定 Demo 边界为准。新增评测只量化既有解析、检索、意图和回答表现，不扩展通用复杂表格、报警知识或真实设备集成。

## 文档维护约定

- 本入口只维护当前状态、阶段导航和阅读路径，不记录详细实施过程。
- 每个阶段使用 `stages/NN-topic.md`，阶段结束后冻结；后续勘误可以修改，但不继续追加新阶段内容。
- 每项有独立问题、代码、配置、数据库或行为变化的改动，在 `changes/` 下建立一份详情，并在[改动索引](changes/README.md)登记。
- 小型文档整理可以只记入索引；需要复现、迁移或回滚的改动必须有详情页。
- 详情页至少记录问题、范围、关键文件、验证、数据影响、Git 提交和回滚方式；可从[改动记录模板](changes/TEMPLATE.md)开始。
- 原始调研材料、API 密钥、中间件卷和本地备份不进入 Git。文档只记录获取或恢复方法，不复制秘密值。
- Git 提交是代码和文档的事实依据；文档中的状态与当前 checkout 冲突时，先核对分支、HEAD 和工作区，再更新文档。

## 新会话交接方式

新会话可以直接使用下面这段话：

> 请先阅读 `docs/iron-ore-rag/README.md`、`docs/iron-ore-rag/changes/2026-08-13-request-level-retrieval-purity.md` 和 `local-data/eval/review/evidence-summary-v1.md`，再检查当前分支、HEAD 和工作区。七个冻结实验臂、48 条人工盲评、D0 冻结索引对照和 D1 重建结果都不要覆盖或回填。XLSX 分块与第一轮检索纯度优化已完成；若继续，只做请求级去重后的公平补位和改写可重复性验证。阶段 2/3 页面验收仍是独立待办。

如果下一阶段已经完成，应先更新本页的“当前阶段”“最近代码检查点”和“下一阶段”，再开启新的会话。
