# 请求级检索预算与结构化重排

## 记录信息

| 项目 | 内容 |
| --- | --- |
| 日期 | `2026-08-13` |
| 所属阶段 | 冻结评测后的证据驱动优化，不改变阶段 2/3 产品边界 |
| 状态 | 代码、Java 回归、冻结索引 D0 和干净重建 D1 均已完成 |
| 分支 | `research/iron-ore-rag` |
| Git 提交 | `fd538a8`（检索纯度）；XLSX 分块依赖 `ed2590e` |

## 问题与边界

C-final 的 Context Precision 只有 `17.1%`。逐题检查发现，21 道可回答/近领域题中有 17 道被改写为 2～3 个子问题；旧实现把配置的 TopK=10 发给每一个子问题，再把结果合并。因此返回块数不是请求级 10，而是平均 `13.05`、最大 `19`，17/21 道题超过 10。增加 TopK 只会继续放大噪声和上下文成本。

本次只修复能够由代码和冻结结果证明的三项问题：请求级预算失效、模型输出的 `should_split` 未被执行、rerank 看不到文档名与结构化 embedding text。意图关闭时使用全局作用域是既有的显式配置，不把跨知识库检索本身误判为 bug，也不依据一次在线运行宣称稳定准确率提升。

## 根因

1. `RetrievalEngine` 为每个子问题分别构造完整 `contextTopK=10`，最终上下文随拆分数膨胀；rerank 关闭、失败或跳过时也没有统一的最终上限。
2. 改写协议要求模型输出 `should_split`，但解析代码只要看到 `sub_questions` 数组就拆分，协议字段实际无效，重复子问题也不会去重。
3. 向量入库使用了章节路径和表格结构化文本，但 rerank 只接收展示正文。标准名、sheet 和表格行语义没有进入重排判断；后置元数据补全又排在 rerank 之后。
4. C-final 的 Hit@5 已经较高，低纯度主要表现为“正确块存在，但同一请求又带回大量无关块”，不是召回池不足。

## 实施内容

- 将 TopK 定义恢复为请求级产品契约：多子问题共享 10 条最终上下文额度，候选召回预算和 rerank 候选池仍按每个子问题保留，不用缩小候选池换取表面纯度。
- 新增始终启用的 `FinalTopKPostProcessor`，即使 rerank 未启用、失败或被跳过，单个子问题也不能突破分配额度。
- `MultiQuestionRewriteService` 真实执行 `should_split`；`false` 时只使用 rewrite，`true` 时裁剪空白并按原顺序去重。
- 将元数据补全移动到 rerank 之前，重排输入改为“去扩展名的文档名 + 已落库 embedding text”；展示、引用和最终提示词继续使用原始正文，不把内部排序文本泄漏给用户。
- 评测接口增加 collection、最终分数、Excel sheet 和 cell range 的逐块诊断字段，用于定位跨库混入和排序失败，不改变产品回答接口。

后置处理顺序固定为：`Deduplication(1) → Fusion(5) → MetadataEnrichment(8) → Rerank(10) → FinalTopK(15)`。

## 验证

### Java 回归

针对新增行为的 11 个测试先通过，随后运行检索相关的 78 个测试，全部通过。覆盖内容包括：

- 两/三个子问题的请求级额度分配；
- rerank 关闭或失败后的最终上限；
- 元数据在 rerank 前可用，展示正文与 ranking text 隔离；
- `should_split=false`、重复子问题与异常 JSON 的解析；
- Vector、Keyword、Graph 通道、作用域、融合和已有排序行为的回归。

### D0：冻结索引，只替换检索代码

D0 直接复用 C-final 的 `123 + 14 + 4 = 141` 个冻结块，不重新解析或生成向量，因此它用于隔离检索代码的影响。意图和 OCR 均关闭，题集仍为固定 24 题，其中 21 题参与可回答指标。

| 指标 | C-final | D0 | 变化 |
| --- | ---: | ---: | ---: |
| 平均返回块数 | 13.05 | 6.52 | -50.0% |
| 最大返回块数 | 19 | 10 | -9 |
| 超过请求级 TopK=10 的题数 | 17/21 | 0/21 | -17 |
| Context Precision | 17.1% | 29.2% | +12.2 个百分点 |
| 路由纯度 | 73.6% | 84.8% | +11.3 个百分点 |
| 文档召回 | 97.6% | 97.6% | 不变 |
| Anchor Recall | 86.5% | 84.1% | -2.4 个百分点 |
| 锚点 Hit@5（至少一个） | 95.2% | 90.5% | -4.8 个百分点 |

分族 Context Precision 均高于 C-final：XLSX `27.9% → 50.7%`、原生 PDF `7.0% → 14.0%`、扫描 PDF `11.7% → 20.7%`、跨领域 `26.2% → 33.8%`。这说明收益不是单一文档族偶然抬高总体均值。

自动 Hit@5 的下降只发生在 `cross-scope-01`。D0 返回的第 3 块已经是 YS/T 360.3 的标准标题“钛铁矿精矿化学分析方法第3部分：氧化亚铁量的测定重铬酸钾滴定法”，文档召回仍为 100%，但固定锚点使用“钛铁矿精矿中氧化亚铁量的测定方法”，严格字符串规则判为未命中。为保持冻结协议，没有事后修改题集或回填自动分数；记录时同时保留原始指标与这条人工判读。

D0 的检索 P95 从 `8.5 s` 变为 `16.8 s`。两次运行时间不同且重写、向量和 rerank 都依赖在线模型服务，因此不能把单次延迟变化归因于本次本地代码。

### D1：从 seed 干净重建，组合验证

D1 使用同一 OCR 关闭配置重新导入三份文档。XLSX 在线结果与离线结果一致：79 块、最大 1,019 字符、0 块超过 1,024、0 个多余精确重复块、解析锚点 5/5。两份 PDF 本次生成 16 和 6 块，而冻结索引是 14 和 4 块，说明 MinerU 远端结果存在时间漂移；D1 总体 PDF 指标不能单独归因于本次代码。

XLSX 六题的可比结果如下：

| 指标 | C-final 旧分块/旧检索 | D0 旧分块/新检索 | D1 新分块/新检索 |
| --- | ---: | ---: | ---: |
| 锚点 Hit@5（至少一个） | 100.0% | 100.0% | 100.0% |
| 锚点 Hit@5（全部） | 83.3% | 83.3% | 83.3% |
| Anchor Recall | 100.0% | 100.0% | 94.4% |
| Context Precision | 27.9% | 50.7% | 39.3% |
| 文档召回 | 100.0% | 100.0% | 100.0% |

D1 唯一丢失的 XLSX 锚点是 `xlsx-hard-06` 中的“称取 0.2g 烘干矿样”。该内容仍存在于正确 sheet 的 `B10:H14` 块中，但问题被拆为方法、称样量、熔融条件三个子问题后，块未进入最终请求上下文。这暴露了下一项明确限制：子问题分别截断后再去重，会留下未使用的请求额度，却不会从各子问题候选中回填。后续若继续优化，应实现请求级去重后的公平补位或固定改写输出进行重复试验，而不是简单增大最终 TopK。

完整 D1 总体指标低于 D0，主要下降发生在本次重新解析后发生块形状变化的两类 PDF；因此不作为检索代码失败或成功的单因果证据。D1 的数据库和结果哈希已保存到 `local-data/eval/snapshots/current-D1-chunk-purity/`，该目录受 Git 忽略并只在本机保留。

## 可用于简历的保守表述

可写：

> 基于 3 类工业文档和 24 条固定问题构建可审计 RAG 评测；修复多问题检索的请求级 TopK 失效及结构信息未进入重排的问题，在冻结索引上将平均上下文块数从 13.05 降至 6.52、Context Precision 从 17.1% 提升至 29.2%，同时保持 97.6% 文档召回。

必须同时准备解释：样本只有 3 份文档；指标依赖严格字符串锚点；在线模型有运行波动；D0 的自动 Anchor Recall 小幅下降；这些数字不是通用行业基准，也不是完整回答准确率。

## 数据与兼容性

- 无 Schema、Redis 或对象存储配置变化；D0 不需要重新入库。
- 新增的评测响应字段只在 `ragent.eval.enabled=true` 的隔离评测接口生效。
- 结构化 rerank 复用已有 `embedding_text`，不改变展示正文和引用来源。
- XLSX 分块改动必须重新入库才能生效；D1 的块 ID 和向量不能与 C-final 混用。

## 回滚

检索改动执行 `git revert fd538a8`；XLSX 分块执行 `git revert ed2590e`。如果需要恢复运行态，再从 `current-C-final` 或 `current-seed` 的本地 dump 恢复 `ragent_eval_current`。代码回滚不会自动替换已生成的块和向量。
