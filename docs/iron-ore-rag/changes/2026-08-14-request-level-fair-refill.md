# 请求级公平回填固定回放评测

## 记录信息

| 项目 | 内容 |
| --- | --- |
| 日期 | `2026-08-14` |
| 所属阶段 | D1 检索纯度后的单变量 D2 验证 |
| 状态 | 实现和固定回放已完成；门槛失败，默认关闭并停止本轮调参 |
| 分支 | `research/iron-ore-rag` |
| Git 提交 | `dcd9222`（公平回填）、`fa1bc3f`（固定回放）、`d2f0b1f`（配对恢复门槛）、`37fc9d0`（目标锚点脱敏） |

## 结论

公平回填解决了 D1 已知的“候选充足但请求额度未用完”问题，但没有通过质量门槛。三次固定回放中，开启后每次都把 24 题的请求级额度从 `167` 个最终唯一块、`73` 个空位补为 `240` 个、`0` 个空位，涉及的 `19/24` 题全部补满；同时 Hit@5、Context Precision 和路由纯度的中位数均下降。因此 `rag.search.request-level-refill-enabled` 保持 `false`，不把“填满 TopK”写成检索质量提升。

## 设计边界

- 请求级 TopK=10 仍先近似均分给各子问题；开启时不在子问题阶段提前丢弃候选池。
- 第一轮按子问题轮询取得全局唯一块并满足初始配额；第二轮继续轮询各题的剩余候选，直到请求级 TopK 填满或候选耗尽。
- 不横向比较不同 query 的 rerank 分数；最终提示词、来源和 grounding 共用同一份请求级唯一块列表。
- `RetrievalSelectionDiagnostics` 记录候选数、旧前缀唯一数、补位数、最终唯一数和空位数。评测报告会校验诊断值与 off/on 标签一致，防止只改标签而未切换后端。
- 开关默认关闭；D1 的请求级上限、去重、结构化 rerank 和 `should_split` 修复不因关闭回填而撤销。

## 固定回放协议

D2 只比较公平回填开关，不重新改写、不重新入库：

1. 复用 `current-D1-chunk-purity` 的同一数据库和 `D1-chunk-and-purity/setup.json`；off/on 之间只重启后端并切换开关，不调用 `prepare_kb.py`。
2. 从 D1 检索报告读取 24 题已经产生的 `subIntents`，经 `POST /rag/eval/replay` 绕过在线改写模型。六份报告必须逐题精确一致。
3. 固定 `intent=off`、`ocr=off`、TopK=10、相同模型与检索配置，`concurrency=1`。
4. off 和 on 各顺序运行三次，共 `6 × 24 = 144` 次检索；比较器要求重复编号恰为 1、2、3，并拒绝不兼容或有失败项的报告。

回放源为 Git 忽略的 `local-data/eval/runs/D1-chunk-and-purity/retrieval.json`，其 SHA-256 为 `629630e6ef1d4d9bcfd0a2b149e4bacbf45c0f4804b6acf05c15556f30258142`。加载固定 `subIntents` 时强校验以下三项兼容性 SHA：

| 锁定输入 | SHA-256 |
| --- | --- |
| 数据集 | `7a451a17c490cf597a10533f6481ccc8f4859a24d3c0a3eebf9a9999902d973e` |
| 语料清单 | `b1ac47a961806bb3355f7e1249774e85830250b6730f923dad6bb9ced135eb04` |
| D1 setup manifest | `4bac7a56a7a47125bcb05fa19a413e2b5fbcd4ff8cc3b0fbb6b1ed09024aba0a` |

六份运行报告记录的服务端提交均为 `fa1bc3f001d0926e0b1a9ba4dea954fc8ecf9c70`；`d2f0b1f` 和 `37fc9d0` 是运行后对比较器门槛与公开输出的修正，不改变服务端行为。这些约束隔离了在线改写和索引重建漂移，但 embedding、intent 和 rerank 服务仍为在线调用，因此保留三次重复和中位数口径。

## 预注册门槛

比较器先要求六份 24 题报告完整、固定 `subIntents` 精确一致、最终唯一数不超过 TopK，并要求 on 在候选足够时填满 TopK。质量门槛为：

- 目标题 `xlsx-hard-06` 在锁定数据集中的目标锚点（`reference_anchors[1]`）须由 off 缺失变为 on 命中，配对恢复至少 `2/3`，且该题 Anchor Recall 不下降；锚点原文不进入 Git；
- 总体 Anchor Recall 中位数至少增加 `0.01`，文档召回和 Hit@5（至少一个）不下降；
- Context Precision、路由纯度的中位数降幅分别不得超过 `0.02`；
- P95 延迟只记录，不参与放行。

## 结果

下表均为三次重复中位数，质量指标只统计 21 道可回答/近领域题：

| 指标 | off | on | 结论 |
| --- | ---: | ---: | --- |
| 锚点 Hit@5（至少一个） | `0.905` | `0.762` | 下降 `0.143`，失败 |
| Anchor Recall | `0.841` | `0.865` | 增加 `0.024`，通过 |
| Context Precision | `0.254` | `0.200` | 下降 `0.054`，失败 |
| 文档召回 | `1.000` | `1.000` | 不下降，通过 |
| 路由纯度 | `0.915` | `0.811` | 下降 `0.104`，失败 |
| 检索 P95 | `6542 ms` | `9584 ms` | 仅记录 |

逐题中位数也不支持默认开启：Hit@5 为 `0/21` 改善、`3/21` 退化；Context Precision 为 `2/21` 改善、`15/21` 退化。机制诊断则稳定通过：off 三次均为 `167` 个最终唯一块和 `73` 个空位；on 三次均为 `240` 个最终唯一块和 `0` 个空位，补位中位数为 `73`（范围 `73～74`），每次均补满原先有候选却留空的 19 题，且无 TopK 越界。

修正后的目标题门槛同样失败：off 三次已经全部命中，on 的命中不能算作从 off 到 on 的配对恢复，恢复次数为 `0/3`。这也说明不能用一次 D1 的缺失锚点为后续机制预设收益。

最终 gate 为 `fail`，失败项是目标题配对恢复、总体 Hit@5 不回退、Context Precision 容差和路由纯度容差。填入更多低位候选扩大了上下文，但本轮证据没有证明这些新增块提高了前部命中或纯度。

## 数据、回滚与下一步

- 六份原始报告和聚合比较位于 `local-data/eval/runs/D2-request-level-refill/`，受 Git 忽略；不含在本文中，也不公开原始问题、子问题或锚点文本。
- 运行态回滚只需保持或恢复 `rag.search.request-level-refill-enabled=false` 并重启后端，无需重建知识库。
- 若需要移除代码，按逆序回滚 `d2f0b1f`、`fa1bc3f`、`dcd9222`；数据库 Schema、块和向量均无需变化。
- 本阶段按 stop gate 结束，不继续放宽门槛或重复调参。若未来重新立项，优先验证“保留旧版头部顺序，只把补位证据追加到全局尾部或独立追加段”，并重新预注册相同的固定回放和非退化门槛。
