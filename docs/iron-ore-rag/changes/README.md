# 工业文档 RAG 改动索引

本页只映射“改过什么、对应哪次提交、到哪里看原因与证据”。发现过程、根因、方案取舍、实现、效果、限制和回滚均放在单项详情中。

## 秋招冲刺 W1—W5

更早的改动说明（2026-08 至 2026-09-15）已随瘦身删除，由 `slim-v0-baseline` 标签留存。

| 日期 | 改动 | Git 检查点 | 详情 |
| --- | --- | --- | --- |
| 2026-09-18 | 研究 Agent 改为稳定前缀、阈值压缩与百炼显式缓存断点，调用台账补齐缓存写入量与时延，并在同一负载上做前后对照 | 探针 `126820a`；实现 `b186852`；台账与报告 `73deab5`；X1 结果 `4789b45`；标签 `career-w1` | [研究 Agent 提示缓存](2026-09-18-prompt-cache-stable-prefix.md) |
| 2026-09-18 | 新增 OpenAI 兼容的模拟上游与故障注入基准，embedding 重试改为抖动退避，研究路径失败计入候选熔断；在模拟上游上做治理前后对照 | 模拟上游 `4ace8c0`；退避 `7b3d86e`；熔断 `77ca3ae`；X3 准备 `b868c59`、`bd44a2e`；标签 `career-w3` | [不稳定上游的故障注入基准](2026-09-18-upstream-fault-injection.md) |
| 2026-09-18 | 研究长任务改为心跳租约、数据库轮询跨实例接管、由持久事件断点续跑与步边界优雅停机，修复实例启停误杀其他实例任务；多进程故障注入验证 | 止血 `0ea6060`；租约与接管 `30e5cb6`；续跑 `f3ed6b7`；停机 `51c7ea7`、`0c47515`；X2 `cb42dc4`；标签 `career-w2` | [研究长任务的持久化执行](2026-09-18-durable-research-execution.md) |
| 2026-09-18 | 知识库加可见性与授权，由唯一判定入口在召回之前裁剪问答范围，研究任务在创建和每次领取时校验权限，停止接口校验任务归属，运维接口限定 admin；越权矩阵与召回前过滤的集成测试 | 可见性与授权 `cd20e6b`；检索 `71c4389`；研究 `35552f5`；停止 `785a7de`；运维 `412ec2b`；测试 `a21beb5`；标签 `career-w4` | [企业资料的权限隔离](2026-09-18-knowledge-base-access-control.md) |
| 2026-09-18 | 重新入库前按（真实模型、维度、向量文本 SHA-256）查嵌入缓存，只把变化的块去重后送上游；核对并用集成测试证明块表与 pgvector 的先删后插在同一事务；X5（真实上游 SiliconFlow，单次）重复入库 0 次上游调用、V1.2→V1.3 重嵌入 2/79 块，计费 token 45,722→1,368 | 实现 `80125b0`；测试 `7d43998`；X5 `2355966`；标签 `career-w5` | [内容寻址的 embedding 复用](2026-09-18-content-addressed-embedding-reuse.md) |
